# --- Authentication (at least one required) ---
# Claude subscription (Pro/Max) - run `claude setup-token` to get this
CLAUDE_CODE_OAUTH_TOKEN=
# OR pay-per-use API key from console.anthropic.com
# ANTHROPIC_API_KEY=

# --- API Provider Override (optional) ---
# Route agent API calls through an alternative provider instead of Anthropic directly.
# When ANTHROPIC_BASE_URL is set, CLAUDE_CODE_OAUTH_TOKEN is ignored in containers
# and ANTHROPIC_AUTH_TOKEN is used instead. Leave commented out to use Anthropic directly.
# Examples: Z.AI (https://api.z.ai/api/anthropic), OpenRouter, etc.
# ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# API_TIMEOUT_MS=3000000
#
# Provider model mapping overrides (Z.AI specific)
# Controls which GLM model each Claude alias resolves to on the provider side.
# Default mapping (no overrides needed): opus/sonnet → GLM-4.7, haiku → GLM-4.5-Air
# Uncomment to use GLM-5 for opus (requires Z.AI Pro or Max plan):
# ANTHROPIC_DEFAULT_OPUS_MODEL=glm-5
# ANTHROPIC_DEFAULT_SONNET_MODEL=glm-4.7
# ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-4.5-air

# --- Channel Configuration ---
# Set credentials for the channels you want active at runtime.
# At least one channel must be configured or the service will not start.

# Discord: Set this token to enable the Discord channel
DISCORD_BOT_TOKEN=

# Discord channels to auto-register at startup.
# Format: id:name:folder[:noTrigger]  (comma-separated, noTrigger = process all messages)
# Example: 123456:Main:main:noTrigger,789012:Family:family
DISCORD_CHANNELS=

# WhatsApp: Enabled automatically when auth session exists in store/auth/
# Run `npm run auth` to set up WhatsApp authentication.

# --- General ---
# ASSISTANT_NAME=Andy
# LOG_LEVEL=info
# CONTAINER_TIMEOUT=1800000
# MAX_CONCURRENT_CONTAINERS=5

# --- Agent Configuration ---
# Control Claude Agent SDK query options for cost management and model selection.
# All options are optional. If unset, uses SDK defaults (current model, no budget limit).

# Default model for all channels (use model alias: opus, sonnet, haiku, or full model ID)
# AGENT_MODEL=sonnet

# Maximum budget per query in USD (e.g., 0.50 for 50 cents)
# AGENT_MAX_BUDGET_USD=0.50

# Maximum conversation turns per query (safety limit for runaway loops)
# AGENT_MAX_TURNS=30

# Maximum extended thinking tokens (for reasoning models)
# AGENT_MAX_THINKING_TOKENS=10000

# Per-channel model overrides (comma-separated, format: folder:model-alias)
# Overrides AGENT_MODEL for specific channels (use opus, sonnet, haiku)
# Example: family:haiku,devwork:opus
# AGENT_CHANNEL_MODELS=
